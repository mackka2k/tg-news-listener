feat: add smart deduplication and docker support

This major update introduces professional-grade features:

🚀 Smart Deduplication
- Added `bot/deduplication.py` using `thefuzz` library
- Implemented fuzzy string matching (85% threshold)
- Prevents duplicate news from different sources
- Integrated into message processing pipeline

🐳 Docker Support
- Added multi-stage `Dockerfile` (optimized size)
- Added `docker-compose.yml` for easy deployment
- Added `.dockerignore` for security

📊 Performance Monitoring
- Added `bot/performance.py` for granular metrics
- Tracks processing time, DB queries, AI latency
- Exposed via `/stats` endpoint

🛡️ Error Handling
- Created custom exception hierarchy in `bot/exceptions.py`
- Implemented standardized error codes
- Improved logging with structured data

🔧 Infrastructure
- Added database migration framework
- Added CI/CD pipeline (GitHub Actions)
- Reorganized project structure

Documentation:
- Updated `README.md` with Docker instructions
- Updated `CHANGELOG.md` with all new features
- Created `docs/ENVIRONMENT.md` reference
